# AETHRA AI — environment configuration
# All configuration and secrets are read from environment variables only.
# Never hardcode secrets in source files, and never commit real values here.
# Copy this file to .env (gitignored) and fill in local values.

# Path to the SQLite database file (durable persistence).
# Defaults to <project-root>/.data/aethra.db when unset.
AETHRA_DB_PATH=.data/aethra.db

# Scheduler mode:
#   lazy     — production/serverless-safe: durable work runs synchronously on
#              /api/agent/state reads (no background processes).
#   interval — local-development fallback: a 1s worker advances due agents
#              even with no client polling.
# Defaults: interval in development, lazy everywhere else.
AETHRA_SCHEDULER=lazy

# --- Example only: future LLM integration --------------------------------
# When live discovery / LLM generation is added, the API key must come from
# the environment (e.g. a deployment secret), never from code or this file:
# AETHRA_LLM_API_KEY=sk-...
